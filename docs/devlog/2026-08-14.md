# 2026-08-14

- USB를 다시 연결하는 과정에서 라이다/ESP32 물리 포트가 이전과 달라짐. 두 장치
  모두 동일한 CP2102 브릿지(10c4:ea60, serial "0001")라 vendor/product ID로는
  구분 불가 — 케이블을 하나씩 뽑아보는 방식으로 실제 매핑을 확인:
  - 포트 `4-1` (`ttyUSB0`) = ESP32
  - 포트 `2-1` (`ttyUSB1`) = 라이다
  기존 `99-ydlidar.rules`는 `4-1`을 라이다, `2-2`를 ESP32로 잘못 매핑하고
  있었음 (포트 `2-2`는 아예 사용되지 않는 상태였음).
- `b84dd91` 14:28 — `99-ydlidar.rules`의 KERNELS 매핑을 실제 배선에 맞게 수정
  (`2-1` → ydlidar, `4-1` → esp32). `/etc/udev/rules.d/`에도 반영하고
  `udevadm control --reload-rules && udevadm trigger`로 재적용. 이제
  `/dev/ydlidar`, `/dev/esp32` 심볼릭 링크가 정상적으로 올바른 장치를 가리킴.
- `src/ydlidar_ros2_driver/params/ydlidar.yaml`의 `port: /dev/ttyUSB0`를
  `/dev/ydlidar`로 수정 (이 디렉토리는 업스트림 클론이라 `.gitignore`로 이
  저장소에서는 추적 안 됨). 실제 사용하는 `config/ydlidar_g2.yaml`은 이미
  `/dev/ydlidar`로 되어 있어서 손댈 필요 없었음.
- `9e3d704` 14:29 — 2026-08-10 실차 테스트에서 실측한 구동륜 반지름 0.30m을
  반영. `urdf.xacro`와 `base_driver.py`의 `wheel_radius` 기본값을 0.15 →
  0.30으로 수정. `lidar_z`(base_link 기준 상대값, 0.35)는 그대로 두어서
  라이다의 지면 기준 실제 높이가 `wheel_radius + lidar_z = 0.65m`로 자동
  반영되도록 함. xacro 파싱 확인 완료.
- ESP32/라이다 재연결 후 실제 통신 확인: `base_driver` 노드가 `/dev/esp32`를
  오류 없이 열었고, `ydlidar_ros2_driver_node`는 `/dev/ydlidar`에 정상 연결되어
  G2B(Firmware 3.5, Serial 2022061100071019) 인식 후 `/scan`을 ~9.7Hz로 안정
  발행함 (설정 10Hz에 근접, checksum error 1건은 일시적 노이즈로 판단, 이후
  스캔 계속 정상).
- **전진/후진 속도 비대칭 원인 확인**: 코드가 아니라 데드밴드 보정값 문제였음.
  `esp32_joystick_emulator.ino`의 `THROTTLE_DEADBAND_FWD`가 0(보정 없음)인 반면
  `THROTTLE_DEADBAND_REV`/`STEERING_DEADBAND`는 0.45(명령 즉시 0.45 지점까지
  점프 후 1.0까지 램프)로 되어 있어서, 같은 정규화 명령값이라도 후진/조향이
  DAC 편차를 2.3~2.5배 더 크게 받아 훨씬 빠르게 반응하고 있었음
  (`wasd_teleop`의 `LINEAR_SPEED=0.15`/`ANGULAR_SPEED=0.3`, `base_driver` 기본
  `max_linear=0.6`/`max_angular=0.9` 기준: 전진 DAC 편차비 0.25, 후진 0.5875,
  회전 0.633). base_driver.py의 `_send_motor`는 전후진 완전 대칭이라 여긴
  문제 없음 확인.
  2026-08-10 데드밴드 실측 당시 전진은 0.1에도 반응해서 보정을 0으로 뒀던
  것이 원인 — 후진/조향만 보정하고 전진은 그대로 둔 상태였음.
  **잠정 조치**: `THROTTLE_DEADBAND_FWD`를 실측 없이 일단 후진과 동일한
  0.45로 맞춰서 좌우 비대칭을 완화. 전진 자체의 정확한 데드밴드는 아직
  실측되지 않았으므로 근사치이며, 다음 실차/거치대 테스트에서 전진도 0.1,
  0.2, 0.3... 단계별로 반응 지점을 실측해서 갱신 필요.

## TODO
- [ ] 전진 데드밴드(`THROTTLE_DEADBAND_FWD`) 실측 — 현재 0.45는 후진값을
      그대로 가져온 잠정치. 바퀴 들어놓고 단계별로 반응 지점 확인 후 갱신.
      우회전(steering<0) 데드밴드도 아직 미실측 (좌회전과 동일하다고 가정 중).
- [ ] 휠 엔코더 부품 배송 대기 중. 도착하면 `E,<ltick>,<rtick>\n` 프로토콜을
      펌웨어와 base_driver.py 양쪽에 구현 필요 (자세한 부품/방식 결정은
      2026-08-10 devlog 참고).
