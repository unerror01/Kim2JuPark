# 2026-08-10

- `0c1a486` 16:47 — `autonomous_wheelchair` ROS 2 Jazzy 패키지 초기 커밋
  (base_driver, safety_monitor, waypoint_navigator, urdf, launch, config 등)
- `833da12` 18:41 — 사람이 타서 저속으로 테스트할 수 있는 WASD 키보드 텔레옵
  스크립트(`wasd_teleop.py`) 추가. `/cmd_vel` 발행, base_driver의 0.5s
  cmd_timeout 워치독에 의존해서 스크립트가 죽거나 연결이 끊기면 자동 정지.
- `71a6d98` 18:56 — 위 스크립트가 `scripts/`에만 있고 `setup.py`
  `console_scripts`에 등록이 안 돼 있어서 `ros2 run`으로 실행이 안 되던 버그
  수정. 패키지 모듈 안으로 옮기고 entry point 추가.
- 실차 테스트: wasd_teleop으로 실행 자체는 됨. **전진(w) 속도가 후진(s)/좌우
  회전(a,d)에 비해 눈에 띄게 느림** — 코드상 `LINEAR_SPEED`는 전후진 동일값
  (0.15 m/s)이므로 소프트웨어 문제가 아니라 base_driver의 시리얼 명령 매핑
  또는 ESP32 펌웨어(조이스틱 에뮬레이션 전압) 쪽 비대칭 문제로 추정됨.
  다음 작업에서 확인 필요.

## TODO
- [ ] 전진/후진 속도 비대칭 원인 조사 (base_driver.py 시리얼 송신 로직,
      firmware/esp32_joystick_emulator DAC 전압 매핑 확인)
- [ ] 실측 결과 구동륜 반지름 0.30m (기존 코드는 0.15m 하드코딩) — `urdf.xacro`의
      `wheel_radius`, `base_driver.py`의 `wheel_radius` 파라미터 기본값 수정 필요.
      wheel_radius가 바뀌면 base_link 지상고 계산도 바뀌므로, 라이다 장착
      높이(바닥 기준)도 0.50m -> 0.65m로 다시 잡아야 함 (lidar_z=0.35는 그대로,
      base_link가 바닥에서 wheel_radius만큼 뜬다는 전제라서).
- [ ] 휠 엔코더 구매/장착 예정. 최종 결정: **타이어 사이드월에 마찰 롤러를
      눌러 붙이는 방식** (축 나사까지 선을 안 끌고 가도 돼서 배선이 짧아짐,
      롤러 지름이 작아 기어업 효과로 해상도도 좋아짐).
      부품: 소형 고무 롤러(지름 30~40mm) x2, 인크리멘탈 로터리 엔코더
      (200~360 P/R, quadrature A/B, 5V) x2, 플렉시블 샤프트 커플러 x2,
      텐션 스프링 x2, 5V<->3.3V 로직레벨 변환 모듈, 브라켓 재료(3D프린트/
      알루미늄 앵글).
      엔코더 신호는 ESP32 GPIO(PCNT 하드웨어 펄스카운터 권장)로 읽어서
      "E,<ltick>,<rtick>\n" 프로토콜로 시리얼 송신 — 이 프로토콜은 지금
      BUILD_SPEC.md 문서에만 있고 펌웨어/base_driver.py 양쪽 다 실제 구현은
      안 되어 있음, 엔코더 장착 후 같이 구현해야 함.
      (검토했던 다른 방식: 액슬 나사에 마그네틱 인코더 직결 — 배선이 바퀴
      바깥까지 나가야 해서 보류. 바닥을 보는 광학 플로우 센서 — 배선은
      가장 간단하지만 조명/노면 의존성 때문에 보류.)
